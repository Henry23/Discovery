   <div class="container marketing">
      <!-- Three columns of text below the carousel -->
      <div class="row">
        <div class="span4">            
          </div><!-- /.span4 --> 
        
        <div class="span4">  
          <br>
                <h1>Datos de los Horarios</h1>
          <br>
          <table  border="1" cellpadding="1" cellspacing="1" class="table table-condensed">
            <tr>
              <th>Origen</th>
              <th>Destino</th>
              <th>Horarios</th>
              <th>Descripcion</th>
            </tr>          
            <% @camino.each do |u| %>
              <tr> 
                <td><%= Terminal.find_by_id(u.terminal_id_origen).nombre %> </td>
                <td><%= Terminal.find_by_id(u.terminal_id_destino).nombre %> </td>
                <td>sale <%= u.hora_salida.strftime("%H:%M") %>
                lle. <%= u.hora_llegada.strftime("%H:%M") %></td>
                <td>Bus: duración: <%= u.hora_salida - u.hora_salida %>) </td>
            </tr>
            <% end %>                    
          </table>                  
       </div><!-- /.span4 --> 
      </div><!-- /.row -->
   </div>
  